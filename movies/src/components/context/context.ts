/* import { createContext, Dispatch, SetStateAction } from "react";

export const AppContext = createContext({
    stateFormProfile: <Dispatch<SetStateAction<string>>>(state => state
  )}); */

export const teste = {}