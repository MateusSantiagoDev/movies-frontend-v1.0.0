export type LoginRequest = {
    email: string;
    password: string;
};

export type profileRequest = {
    movie: string;
    serie: string;
    anime: string;
}

export type formRequest = {
    title: string;
    description: string;
    avaliation: number;
    image: string;
}